사실 아직도 복습을 하고 계속 생각을 하지만 

이문제는 가장 간단한 문제중 하나다 


class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        
        a = {}
        
        
        for i, num in enumerate(nums):
            if target - num in a:
                return([a[target - num], i])
            elif num not in a:
                a[num] = i
